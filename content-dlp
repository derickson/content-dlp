#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VENV_DIR="$HOME/.venvs/content-dlp"
[ -d "$VENV_DIR" ] || { echo "Error: venv not found at $VENV_DIR" >&2; exit 1; }
exec "$VENV_DIR/bin/python" -m content_dlp "$@"
